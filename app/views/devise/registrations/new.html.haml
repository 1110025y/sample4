.contents
  = form_with model: @user, url: user_registration_path, id: 'new_user', class: 'new_user', local: :true do |f|
    .contents__registrations
      .registrations-title Rider’s Garage会員登録
      .registrations-text
        %i.fas.fa-dot-circle#fa-dot-circle
        %p.text-ltc ユーザー名/メールアドレス/パスワード
      .registrations-fromBox
        .fromBox
          .field-n
            .field-n--nameTitle
              .fields
                = f.label :ニックネーム
                %span.Required 必須
            .field-n--nameForm
              %p.nameForm-mge <ご自身のニックネームを入力してください>
              = f.text_field :nickname, placeholder: "例)バイカー", autofocus: true, autocomplete: "nickname", class: 'nameForm'
          .field-e
            .field-e--emailTitle
              .fields
                = f.label :メールアドレス
                %span.Required 必須
            .field-e--emailForm
              %p.emailForm-mge <半角英数字><br><他の会員が登録済みのメールアドレスは登録できません>
              = f.email_field :email, placeholder: "例)baiku1234", autofocus: true, autocomplete: "email", class: 'emailForm'
          .field-p
            .field-p--passTitle
              .fields
                = f.label :パスワード
                %span.Required 必須
            .field-p--passForm
              - if @minimum_password_length
                %em.Form-mge
                  <6文字以上半角英数字>
              = f.password_field :password, autocomplete: "new-password", class: 'new-pass-Form'
              %p.passForm-mge <確認のためもう一度入力してください>
              = f.password_field :password_confirmation, autocomplete: "new-password", class: 'new-pass-Form'
        
      .registrations-text
        %i.fas.fa-dot-circle#fa-dot-circle
        %p.text-ltc お客様の基本情報
      .registrations-fromBoxes
        .fromBoxes
          .field-ff
            .field-ff--ffTitle
              .fields
                = f.label :氏名
                %span.Required 必須
            .field-ff--ffForm
              %p.ffForm-mge <入力情報に誤りがあると、パスワード再設定が正しく行えない場合があります><br><ご自身の氏名をお間違えないように登録してください>
              .ffForm-box
                = f.label :（姓）, class: 'ffLabel'
                = f.text_field :family_name, placeholder: "例)山田", class: 'ffForm'
                = f.label :（名）, class: 'ffLabel'
                = f.text_field :first_name, placeholder: "例)太郎", class: 'ffForm'
          .field-kana
            .field-kana--kanaTitle
              .fields
                = f.label :氏名（フリガナ）
                %span.Required 必須
            .field-kana--kanaForm
              %p.kanaForm-mge <全角カタカナのみ>
              .kanaForm-box
                = f.label :（姓）, class: 'kanaLabel'
                = f.text_field :family_name_kana, placeholder: "例)やまだ", class: 'kanaForm'
                = f.label :（名）, class: 'kanaLabel'
                = f.text_field :first_name_kana, placeholder: "例)たろう", class: 'kanaForm'
            
      .registrations-actions
        = recaptcha_tags
        = f.submit "登録", class: 'sbtForm'
        %a.backBtn{:href => "/users/new"} もどる
        %a.top-backBtn{:href => "/items"} トップページに戻る

